# Docker 默认仅监听 VPS 本机，不占用公网 80/443。
BIND_ADDRESS=127.0.0.1
HTTP_PORT=51213
SITE_ADDRESS=:80

# 请分别执行 `openssl rand -hex 12` 和 `openssl rand -hex 32` 后填入。
YUEMI_INITIAL_PASSWORD=
SMTP_CONFIG_KEY=

# 默认在本机从源码构建。需要使用 GitHub 容器镜像时取消下一行注释。
# YUEMI_IMAGE=ghcr.io/wxd2233/yuemi-vault:latest
